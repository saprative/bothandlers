## 1. App Router Setup

- [ ] 1.1 Move the existing dashboard application files (if any are at root `app/page.tsx`) to `app/dashboard/page.tsx` (and related layouts) to free up the root URL.
- [ ] 1.2 Update the root layout (`app/layout.tsx`) to support dark mode globally by default (e.g., `<html class="dark">`).

## 2. Landing Page UI (Vibe Tech Aesthetic)

- [ ] 2.1 Create the Hero section in `app/page.tsx` featuring the "PagerDuty for AI Agents" value proposition.
- [ ] 2.2 Style the Hero section using Tailwind dark mode classes (black backgrounds, glowing neon text, monospace accents).
- [ ] 2.3 Implement the primary Call-to-Action (CTA) button ("Go to Dashboard" or "Get Started") linking to `/dashboard`.
- [ ] 2.4 Add a feature explanation section illustrating the "AI hits a boundary -> Routes to Human -> Returns decision" loop.
- [ ] 2.5 Polish typography and spacing to match the high-contrast technical aesthetic.
